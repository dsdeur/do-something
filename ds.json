{
  "commands": {
    "install": {
      "aliases": ["i"],
      "command": "cargo build --release && cargo install --path ."
    },
    "build": {
      "aliases": ["b"],
      "command": "cargo build --release"
    },
    "run": {
      "aliases": ["r"],
      "command": "cargo run --"
    },
    "clippy": {
      "aliases": ["c"],
      "command": "cargo clippy"
    },
    "test": {
      "aliases": ["t"],
      "command": "cargo test"
    }
  }
}
