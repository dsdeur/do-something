{
  "commands": {
    "install": {
      "aliases": ["i"],
      "command": "cargo build --release && cargo install --path ."
    },
    "run": {
      "aliases": ["r"],
      "command": "cargo run --"
    },
    "clippy": {
      "aliases": ["c"],
      "command": "cargo clippy"
    }
  }
}
